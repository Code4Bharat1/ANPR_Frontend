import ServerSetup from '@/components/superadmin/serversetup';

export const metadata = {
  title: 'Server Setup - ANPR Super Admin',
  description: 'Configure server and client settings',
};

export default function ServerSetupPage() {
  return <ServerSetup />;
}
